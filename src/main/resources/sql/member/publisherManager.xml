<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sh.model.dao.PublisherManagerMapper">

    <insert id="insertPublisherManager" useGeneratedKeys="true" keyProperty="memberId">
    <insert id="insertPublisherManager" useGeneratedKeys="true" keyProperty="publisherManagerId">
        insert into
            publisher_manager(member_id, publisher_id)
        values(
            #{memberId},
            #{publisherId}
        )
    </insert>

<!--    <select id="findPublisherManager" resultType="PublisherManagerDto">-->
<!--        select-->
<!--            *-->
<!--        from-->
<!--            member m join publisher_manager pm-->
<!--                on m.member_id = pm.member_id-->
<!--        where-->
<!--            publisher_manager_id = #{publisherManagerId}-->
<!--    </select>-->

<!--    <select id="loginCheck" resultType="">-->

<!--    </select>-->

<!--    <select id="findPublisher" resultType="Publisher">-->
<!--        select distinct name-->
<!--        from publisher;-->
<!--    </select>-->
<!--    <delete id="">-->

<!--    </delete>-->
</mapper>